# Optional for public GitHub repositories; required for private repositories.
GITHUB_TOKEN=
# Required only when the Gemini model profile is enabled.
GEMINI_API_KEY=
# Required only when the OpenAI model profile is enabled.
OPENAI_API_KEY=
# SQLite database location used by the backend container.
DATABASE_URL=sqlite:///./data/pr_gate_v2.db
